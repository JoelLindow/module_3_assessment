<%= form_tag("/search", method: "get") do %>
            <%= label_tag(:zip, "Search by zip") %>
            <%= text_field_tag(:zip), params[:zip] %>

            <%= submit_tag("Submit") %>
        <% end %>
